image: microsoft/dotnet:latest

stages:
  - build
  - test

variables:
  dir: "aspnet-core/"

before_script:
  - "export ASPNETCORE_Environment=Development"
  - "cd EduVault"
  - "dotnet publish"

build:
  stage: build
  script:
    - "ls"