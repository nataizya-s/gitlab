image: microsoft/dotnet:latest

stages:
  - build
  - deploy

variables:
  dir: "aspnet-core/"

before_script:
  - export ASPNETCORE_Environment=Development
  - cd EduVault
  - 'which ssh-agent || ( apt-get update -y && apt-get install openssh-client -y )'
  - eval $(ssh-agent -s)
  - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add -
  - mkdir -p ~/.ssh
  - chmod 700 ~/.ssh
  - ssh-keyscan  104.154.235.209 >> ~/.ssh/known_hosts
  - chmod 644 ~/.ssh/known_hosts
  - ssh nataizya_s@104.154.235.209
#build:
 # stage: build
  #script:
   # - "echo 'doing build'"

deploy:
  stage: deploy 
  script:
    - "ls -la"